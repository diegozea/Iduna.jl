var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"section","location":"api/#Main-Entry-Point","page":"API","text":"iduna runs the full ThorAxe plus MMseqs2/HMMER pipeline by default. Use no_expansion=true to stop after the ThorAxe MSA stage; in that case result.expansion === nothing, load_seed_msa(result) remains available, and load_expanded_msa(result) throws a guidance error.","title":"Main Entry Point"},{"category":"section","location":"api/#Result-Types","page":"API","text":"","title":"Result Types"},{"category":"section","location":"api/#Submodules","page":"API","text":"","title":"Submodules"},{"category":"function","location":"api/#Iduna.iduna","page":"API","text":"iduna(; id, mmseqs_db=nothing, no_expansion=false, kwargs...) -> IdunaResult\niduna(id; mmseqs_db=nothing, no_expansion=false, kwargs...) -> IdunaResult\n\nBuild one ThorAxe-based MSA from a UniProt accession or an Ensembl transcript ID and, by default, expand it with MMseqs2/HMMER. Set no_expansion=true to stop after the ThorAxe MSA stage without requiring an MMseqs2 database.\n\nThe function is file-first: every external tool writes its inputs, outputs, and logs under workdir, and the returned IdunaResult stores paths plus the resolved identifiers and validation statistics.\n\northology controls the ThorAxe relationship filter (\"1:1\", \"1:n\", or \"m:n\"). By default, Iduna filters the ThorAxe species list with Ensembl homology and then filters against currently available BioMart Ensembl Gene datasets before transcript_query. transcript_query_timeout_seconds limits each Ensembl query attempt. Iduna retries that step and can retry without specieslist after a timeout. Set thoraxe_timeout_seconds to give the baseline and PID ThorAxe runs the same kind of wall-clock guard. Pass thoraxe_input_dir to reuse a complete transcriptquery bundle instead of fetching it again. Set centroids=true to also save the centroid-level MSA before MMseqs2 expands centroid hits to cluster members; the main expansion and validation still use the full expanded MSA. centroids=true requires expansion and cannot be combined with `noexpansion=true`.\n\n\n\n\n\n","title":"Iduna.iduna"},{"category":"function","location":"api/#Iduna.load_seed_msa","page":"API","text":"load_seed_msa(result; keepinserts=true)\n\nLoad the selected ThorAxe seed MSA from an IdunaResult as a MIToS MSA.\n\n\n\n\n\n","title":"Iduna.load_seed_msa"},{"category":"function","location":"api/#Iduna.load_expanded_msa","page":"API","text":"load_expanded_msa(result; keepinserts=true)\n\nLoad the expanded match-column MSA from an IdunaResult as a MIToS MSA.\n\n\n\n\n\n","title":"Iduna.load_expanded_msa"},{"category":"type","location":"api/#Iduna.Utils.ResolvedTarget","page":"API","text":"ResolvedTarget\n\nResolved identifiers and sequence-validation metadata for one input ID.\n\n\n\n\n\n","title":"Iduna.Utils.ResolvedTarget"},{"category":"type","location":"api/#Iduna.Utils.SeedSelection","page":"API","text":"SeedSelection\n\nThe ThorAxe PID seed chosen for expansion, plus the summary values used to pick it.\n\n\n\n\n\n","title":"Iduna.Utils.SeedSelection"},{"category":"type","location":"api/#Iduna.Utils.ThorAxeMSAResult","page":"API","text":"ThorAxeMSAResult\n\nPaths and metadata produced by the ThorAxe MSA-building stage.\n\n\n\n\n\n","title":"Iduna.Utils.ThorAxeMSAResult"},{"category":"type","location":"api/#Iduna.Utils.ExpansionResult","page":"API","text":"ExpansionResult\n\nPaths and hit counts produced by the MMseqs2/HMMER expansion stage.\n\n\n\n\n\n","title":"Iduna.Utils.ExpansionResult"},{"category":"type","location":"api/#Iduna.Utils.ValidationResult","page":"API","text":"ValidationResult\n\nSeed and expanded-MSA statistics plus optional query-vs-UniProt checks.\n\n\n\n\n\n","title":"Iduna.Utils.ValidationResult"},{"category":"type","location":"api/#Iduna.Utils.IdunaResult","page":"API","text":"IdunaResult\n\nTop-level result returned by Iduna.iduna. It keeps the full pipeline status and links to the per-stage result objects. expansion is nothing when the run was requested with no_expansion=true.\n\n\n\n\n\n","title":"Iduna.Utils.IdunaResult"},{"category":"section","location":"#Iduna","page":"Home","text":"Iduna builds one ThorAxe-based multiple sequence alignment and, by default, expands it with MMseqs2/HMMER. The public API is centered on iduna, which accepts one UniProt accession or Ensembl transcript ID and writes a reproducible work directory.\n\nThe package is file-first. External tools write logs and intermediate files under the chosen workdir, and the returned result stores paths plus the resolved identifiers, selected seed, expansion outputs, validation statistics, warnings, and status. result.workdir is absolute; artifact paths under it are reported relative to workdir.\n\nusing Iduna\n\nresult = iduna(\n    \"P20963\";\n    mmseqs_db=\"/path/to/mmseqs/uniref_db\",\n    workdir=\"P20963\",\n    overwrite=false,\n    centroids=false,\n    transcript_query_timeout_seconds=180,\n)\n\nexpanded = load_expanded_msa(result)\n\nThe same entry point is available as a Julia 1.12 app:\n\njulia --project=. -m Iduna P20963 --mmseqs-db /path/to/mmseqs/uniref_db\n\nAdd --centroids (or centroids=true in Julia) to also save a centroid-level MSA. This is a side output built from MMseqs2 centroid or consensus hits before cluster expansion; the regular expanded MSA remains the main result used by validation.\n\nUse no_expansion=true in Julia, or --no-expansion in the app, to stop after the ThorAxe MSA stage. In that mode mmseqs_db is not required, result.expansion === nothing, and load_seed_msa(result) loads the selected ThorAxe PID seed.\n\nthoraxe_only = iduna(\n    \"ENST00000362089.10\";\n    no_expansion=true,\n    workdir=\"ENST00000362089_thoraxe\",\n)\n\nseed = load_seed_msa(thoraxe_only)\nthoraxe_only.thoraxe_msa.baseline_stockholm\nthoraxe_only.thoraxe_msa.best_seed.stockholm_path\n\njulia --project=. -m Iduna ENST00000362089.10 --no-expansion\n\nFor an Ensembl transcript input, Iduna resolves the parent Ensembl gene ID and species needed by ThorAxe. It does not require UniProt mapping on that path.\n\nIduna filters the ThorAxe species list with Ensembl homology by default using orthology=\"1:1\", then applies biomart_datasets_filter=true as a second preflight against the current BioMart Ensembl Gene dataset list. BioMart dataset names are used only internally; species names are still passed to ThorAxe. Use orthology=\"1:n\" or \"m:n\" for broader ortholog relationships, set specieslist_filter=false to skip the Ensembl homology step, or set biomart_datasets_filter=false to skip the BioMart dataset preflight. The BioMart dataset list is cached in package scratch space and refreshed when used on a later calendar date. Iduna also reports species recorded in transcript_query BioMart failure outputs when a run completes with partial BioMart failures. transcript_query_timeout_seconds defaults to 180 seconds, with a bounded retry that can drop the species list after a timeout. thoraxe_timeout_seconds is unset by default because ThorAxe runtime depends on gene complexity and the selected PID thresholds.\n\nIf the ThorAxe transcript_query bundle has already been created, pass it with thoraxe_input_dir. Iduna copies that bundle into workdir/thoraxe_input and continues with the same ThorAxe MSA and PID seed stages. Unless no_expansion=true, it also runs expansion and expanded-MSA validation.\n\n","title":"Iduna"},{"category":"section","location":"output/#Output-Layout","page":"Output Layout","text":"Iduna keeps ThorAxe's own output layout untouched and writes package metadata around it. By default, workdir is a directory named after the input ID.\n\n<workdir>/\n  target.json\n  result.json\n  logs/\n    thoraxe/\n  sequences/\n    uniprot/\n    ensembl_proteins/\n  thoraxe_input/\n    Ensembl/\n  thoraxe/\n    path_table.csv\n    s_exon_table.csv\n    msa/\n  thoraxe_msa/\n    seeds/\n    best_seed.csv\n  expansion/  # absent when no_expansion=true / --no-expansion\n    <gene>/<transcript>/\n      seeds/\n      dbs/\n      hmm/\n      logs/\n      expanded_msa/\n      centroid_msa/  # optional, written only with centroids=true / --centroids\n  validation/\n    stats.csv\n    query_vs_uniprot_alignment.txt\n\nThe output layout is part of the API. overwrite=true rebuilds package-owned subdirectories, but Iduna does not delete files outside the selected work directory.\n\nAfter the work directory is created, Iduna always attempts to write result.json. Successful runs keep the returned IdunaResult summary there. Failed runs still throw the original exception, but leave a machine-readable result.json with status: \"error\", failed_stage, any available partial target metadata, accumulated warnings, and exception details. Timeout failures from logged ThorAxe commands also include the command and stdout/stderr log paths.\n\nWhen thoraxe_input_dir is supplied, that source bundle is treated as read-only and copied into thoraxe_input/; the copied layout is then preserved like a fresh transcript_query result.\n\nMSA paths are also available from the returned IdunaResult:\n\nresult.thoraxe_msa.baseline_stockholm\nresult.thoraxe_msa.baseline_fasta\nresult.thoraxe_msa.best_seed.stockholm_path\nresult.thoraxe_msa.best_seed.fasta_path\n\n# Full expansion runs only:\nresult.expansion.match_stockholm\nresult.expansion.a3m_path\n\nresult.workdir is stored as an absolute path. Artifact paths inside workdir are stored relative to it, so use joinpath(result.workdir, path) to open them directly. Paths outside workdir remain unchanged.\n\nWhen no_expansion=true or --no-expansion is used, Iduna stops after the ThorAxe MSA stage. result.expansion === nothing, result.json contains \"expansion\": null, and no expansion/ directory is written. Validation still writes seed statistics to validation/stats.csv, with expanded-MSA fields left missing.\n\nexpanded_msa/ is the full MMseqs2 cluster-expanded MSA and remains the main Iduna result. When centroids=true or --centroids is used, Iduna also writes centroid_msa/ with the MSA built from centroid/consensus hits before cluster expansion. This side output reuses the same MMseqs2 search result, does not run a second search, and does not change validation or result.json. Its files are <transcript>_centroids_full.sto, <transcript>_centroids_matchonly.sto, <transcript>_centroids.a3m, and <transcript>_centroid_hits_raw.fasta.","title":"Output Layout"}]
}
