var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"section","location":"api/#Main-Entry-Point","page":"API","text":"iduna runs the full ThorAxe plus MMseqs2/HMMER pipeline by default. Use no_expansion=true to stop after the ThorAxe MSA stage; in that case isempty(result.expansions), load_seed_msa(result) remains available, and load_expanded_msa(result) throws a guidance error.","title":"Main Entry Point"},{"category":"section","location":"api/#Result-Types","page":"API","text":"","title":"Result Types"},{"category":"section","location":"api/#Submodules","page":"API","text":"","title":"Submodules"},{"category":"function","location":"api/#Iduna.iduna","page":"API","text":"iduna(; id, mmseqs_db=nothing, no_expansion=false, kwargs...) -> IdunaResult\niduna(id; mmseqs_db=nothing, no_expansion=false, kwargs...) -> IdunaResult\n\nBuild one ThorAxe-based MSA from a UniProt accession or an Ensembl transcript ID and, by default, expand it with MMseqs2/HMMER. Set no_expansion=true to stop after the ThorAxe MSA stage without requiring an MMseqs2 database.\n\nThe function is file-first: every external tool writes its inputs, outputs, and logs under workdir, and the returned IdunaResult stores paths plus the resolved identifiers and validation statistics.\n\northology controls the ThorAxe relationship filter (\"1:1\", \"1:n\", or \"m:n\"). By default, Iduna filters the ThorAxe species list with Ensembl homology and then filters against currently available BioMart Ensembl Gene datasets before transcript_query. transcript_query_timeout_seconds limits each Ensembl query attempt. Iduna retries that step and can retry without specieslist after a timeout. Set thoraxe_timeout_seconds to give the PID ThorAxe runs the same kind of wall-clock guard. pid_sample_count controls how many PID-specific species samples are drawn from each candidate msa_0 and scored against that PID's full-species MSA; the default is 45. pid_sample_fraction controls the fraction of non-reference species retained in each sample, and pid_sample_seed can make the sampling reproducible. If omitted, a random seed is recorded with the result. Candidate msa_0 reconstructions with indels versus UniProt are reported and excluded from seed selection; substitution-only differences are warnings. Seed selection uses highest median identity, then highest mean identity, then the largest candidate msa_0 species count, then the first PID in pid_thresholds order. Set pid_sample_count=0 to skip seed selection and carry every eligible PID candidate forward. Pass thoraxe_input_dir to reuse a complete transcriptquery bundle instead of fetching it again. Set centroids=true to also save the centroid-level MSA before MMseqs2 expands centroid hits to cluster members; the main expansion and validation still use the full expanded MSA. centroids=true requires expansion and cannot be combined with `noexpansion=true`.\n\n\n\n\n\n","title":"Iduna.iduna"},{"category":"function","location":"api/#Iduna.load_seed_msa","page":"API","text":"load_seed_msa(result; keepinserts=true, pid=nothing, index=nothing)\n\nLoad a selected ThorAxe seed MSA from an IdunaResult as a MIToS MSA.\n\n\n\n\n\n","title":"Iduna.load_seed_msa"},{"category":"function","location":"api/#Iduna.load_expanded_msa","page":"API","text":"load_expanded_msa(result; keepinserts=true, pid=nothing, index=nothing)\n\nLoad an expanded match-column MSA from an IdunaResult as a MIToS MSA.\n\n\n\n\n\n","title":"Iduna.load_expanded_msa"},{"category":"type","location":"api/#Iduna.Utils.ResolvedTarget","page":"API","text":"ResolvedTarget\n\nResolved identifiers and sequence-validation metadata for one input ID.\n\n\n\n\n\n","title":"Iduna.Utils.ResolvedTarget"},{"category":"type","location":"api/#Iduna.Utils.SeedSelection","page":"API","text":"SeedSelection\n\nThe ThorAxe PID seed chosen for expansion, plus the summary values used to pick it.\n\n\n\n\n\n","title":"Iduna.Utils.SeedSelection"},{"category":"type","location":"api/#Iduna.Utils.ThorAxeMSAResult","page":"API","text":"ThorAxeMSAResult\n\nPaths and metadata produced by the ThorAxe MSA-building stage.\n\n\n\n\n\n","title":"Iduna.Utils.ThorAxeMSAResult"},{"category":"type","location":"api/#Iduna.Utils.ExpansionResult","page":"API","text":"ExpansionResult\n\nPaths and hit counts produced by the MMseqs2/HMMER expansion stage.\n\n\n\n\n\n","title":"Iduna.Utils.ExpansionResult"},{"category":"type","location":"api/#Iduna.Utils.ValidationResult","page":"API","text":"ValidationResult\n\nSeed and expanded-MSA statistics plus optional query-vs-UniProt checks.\n\n\n\n\n\n","title":"Iduna.Utils.ValidationResult"},{"category":"type","location":"api/#Iduna.Utils.IdunaResult","page":"API","text":"IdunaResult\n\nTop-level result returned by Iduna.iduna. It keeps the full pipeline status and links to the per-stage result objects. expansions is empty when the run was requested with no_expansion=true.\n\n\n\n\n\n","title":"Iduna.Utils.IdunaResult"},{"category":"section","location":"#Iduna","page":"Home","text":"Iduna builds one ThorAxe-based multiple sequence alignment and, by default, expands it with MMseqs2/HMMER. The public API is centered on iduna, which accepts one UniProt accession or Ensembl transcript ID and writes a reproducible work directory.\n\nThe package is file-first. External tools write logs and intermediate files under the chosen workdir, and the returned result stores paths plus the resolved identifiers, selected seed, expansion outputs, validation statistics, warnings, and status. result.workdir is absolute; artifact paths under it are reported relative to workdir.\n\nusing Iduna\n\nresult = iduna(\n    \"P20963\";\n    mmseqs_db=\"/path/to/mmseqs/uniref_db\",\n    workdir=\"P20963\",\n    overwrite=false,\n    centroids=false,\n    transcript_query_timeout_seconds=180,\n)\n\nexpanded = load_expanded_msa(result)\n\nThe same entry point is available as a Julia 1.12 app:\n\njulia --project=. -m Iduna P20963 --mmseqs-db /path/to/mmseqs/uniref_db\n\nAdd --centroids (or centroids=true in Julia) to also save a centroid-level MSA. This is a side output built from MMseqs2 centroid or consensus hits before cluster expansion; the regular expanded MSA remains the main result used by validation.\n\nUse no_expansion=true in Julia, or --no-expansion in the app, to stop after the ThorAxe MSA stage. In that mode mmseqs_db is not required, isempty(result.expansions), and load_seed_msa(result) loads the selected ThorAxe PID seed.\n\nthoraxe_only = iduna(\n    \"ENST00000362089.10\";\n    no_expansion=true,\n    workdir=\"ENST00000362089_thoraxe\",\n)\n\nseed = load_seed_msa(thoraxe_only)\nthoraxe_only.thoraxe_msa.baseline_stockholms[1]\nthoraxe_only.thoraxe_msa.seeds[1].stockholm_path\n\njulia --project=. -m Iduna ENST00000362089.10 --no-expansion\n\nFor an Ensembl transcript input, Iduna resolves the parent Ensembl gene ID and species needed by ThorAxe. It does not require UniProt mapping on that path.\n\nIduna filters the ThorAxe species list with Ensembl homology by default using orthology=\"1:1\", then applies biomart_datasets_filter=true as a second preflight against the current BioMart Ensembl Gene dataset list. BioMart dataset names are used only internally; species names are still passed to ThorAxe. Use orthology=\"1:n\" or \"m:n\" for broader ortholog relationships, set specieslist_filter=false to skip the Ensembl homology step, or set biomart_datasets_filter=false to skip the BioMart dataset preflight. The BioMart dataset list is cached in package scratch space and refreshed when used on a later calendar date. Iduna also reports species recorded in transcript_query BioMart failure outputs when a run completes with partial BioMart failures. transcript_query_timeout_seconds defaults to 180 seconds, with a bounded retry that can drop the species list after a timeout. thoraxe_timeout_seconds is unset by default because ThorAxe runtime depends on gene complexity and the selected PID thresholds.\n\nSeed selection is per PID threshold. Iduna runs transcript_query once and builds one full-species candidate msa_0 at each PID threshold. Each candidate is validated: indels versus UniProt exclude that PID from selection, while substitutions are reported as warnings. Species samples are drawn from each PID's own candidate msa_0, run at that PID, and scored by HHsuite against the same candidate. Iduna chooses the highest median identity, highest mean identity, largest candidate msa_0 species count, and finally the first PID in pid_thresholds order. By default Iduna uses 45 samples, retains 80% of non-reference species per sample, and records a random pid_sample_seed unless one is supplied. Set pid_sample_count=0 to skip seed selection and carry every eligible PID candidate forward. In that mode result.thoraxe_msa.seeds, result.expansions, and result.validations can contain multiple entries; use pid= or index= with load_seed_msa and load_expanded_msa to select one.\n\nIf the ThorAxe transcript_query bundle has already been created, pass it with thoraxe_input_dir. Iduna copies that bundle into workdir/thoraxe_input and continues with the same ThorAxe MSA and PID seed stages. Unless no_expansion=true, it also runs expansion and expanded-MSA validation.\n\n","title":"Iduna"},{"category":"section","location":"output/#Output-Layout","page":"Output Layout","text":"Iduna keeps ThorAxe's own output layout untouched and writes package metadata around it. By default, workdir is a directory named after the input ID.\n\n<workdir>/\n  target.json\n  result.json\n  logs/\n    thoraxe/\n  sequences/\n    uniprot/\n    ensembl_proteins/\n  thoraxe_input/\n    Ensembl/\n  thoraxe_msa/\n    runs/\n      pid_10.00/\n        full/\n          thoraxe/\n    candidates/\n      pid_10.00/\n        candidate_msa_full.fasta\n        candidate_msa_full.sto\n        scores.csv\n        sequences/\n          candidate_sequences_full.fasta\n          candidate_sequences_species_subset_001.fasta\n        species/\n          candidate_species_full.txt\n          candidate_species_subset_001.txt\n    candidate_summary.csv\n  expansion/  # absent when no_expansion=true / --no-expansion\n    <gene>/<transcript>/\n      pid_10.00/\n        step_state.json\n        seeds/\n        dbs/\n        hmm/\n        logs/\n        expanded_msa/\n        centroid_msa/  # optional, written only with centroids=true / --centroids\n  validation/\n    pid_10.00/\n      stats.csv\n      query_vs_uniprot_alignment.txt\n\nThe output layout is part of the API. overwrite=true rebuilds package-owned subdirectories, but Iduna does not delete files outside the selected work directory.\n\nAfter the work directory is created, Iduna always attempts to write result.json. Successful runs keep the returned IdunaResult summary there. Failed runs still throw the original exception, but leave a machine-readable result.json with status: \"error\", failed_stage, any available partial target metadata, accumulated warnings, and exception details. Timeout failures from logged ThorAxe commands also include the command and stdout/stderr log paths.\n\nWhen thoraxe_input_dir is supplied, that source bundle is treated as read-only and copied into thoraxe_input/; the copied layout is then preserved like a fresh transcript_query result.\n\nthoraxe_msa/runs/ keeps the raw ThorAxe output for each PID candidate and its PID-specific species samples. thoraxe_msa/candidates/ stores each PID's full-species candidate msa_0, sampled species lists, gap-free sequence files, and per-PID score CSV. thoraxe_msa/candidate_summary.csv records validation status, eligibility, sample identity statistics, candidate size, and the selected seed rows.\n\nMSA paths are also available from the returned IdunaResult:\n\nresult.thoraxe_msa.baseline_stockholms[1]\nresult.thoraxe_msa.baseline_fastas[1]\nresult.thoraxe_msa.seeds[1].stockholm_path\nresult.thoraxe_msa.seeds[1].fasta_path\nresult.thoraxe_msa.seeds[1].s_exon_blocks_tsv\nresult.thoraxe_msa.pid_sample_count\nresult.thoraxe_msa.pid_sample_fraction\nresult.thoraxe_msa.pid_sample_seed\n\n# Full expansion runs only:\nresult.expansions[1].match_stockholm\nresult.expansions[1].s_exon_blocks_tsv\nresult.expansions[1].a3m_path\n\nresult.workdir is stored as an absolute path. Artifact paths inside workdir are stored relative to it, so use joinpath(result.workdir, path) to open them directly. Paths outside workdir remain unchanged.\n\nWhen no_expansion=true or --no-expansion is used, Iduna stops after the ThorAxe MSA stage. isempty(result.expansions), result.json contains an empty \"expansions\" list, and no expansion/ directory is written. Validation still writes seed statistics to validation/pid_<value>/stats.csv, with expanded-MSA fields left missing.\n\nBy default, Iduna selects one PID seed. Set pid_sample_count=0 to skip seed selection and carry every eligible PID candidate forward, producing one validation directory and, unless no_expansion=true, one expansion directory per selected PID.\n\nexpanded_msa/ is the full MMseqs2 cluster-expanded MSA and remains the main Iduna result. When centroids=true or --centroids is used, Iduna also writes centroid_msa/ with the MSA built from centroid/consensus hits before cluster expansion. This side output reuses the same MMseqs2 search result, does not run a second search, and does not change validation or result.json. Its files are <transcript>_centroids_full.sto, <transcript>_centroids_matchonly.sto, <transcript>_centroids.a3m, <transcript>_centroids_s_exon_blocks.tsv, and <transcript>_centroid_hits_raw.fasta.","title":"Output Layout"},{"category":"section","location":"output/#S-Exon-Column-Annotations","page":"Output Layout","text":"Iduna keeps track of which original ThorAxe s-exon each MSA column came from. This is useful when you want to color the alignment by s-exon blocks, compare blocks between the seed and expanded MSA, or inspect which part of the original ThorAxe path supports a region of interest.\n\nFor most users, the easiest file to use is the block table:\n\nseed_blocks = joinpath(\n    result.workdir,\n    result.thoraxe_msa.seeds[1].s_exon_blocks_tsv,\n)\n\nexpanded_blocks = joinpath(\n    result.workdir,\n    result.expansions[1].s_exon_blocks_tsv,\n)\n\nEach *_s_exon_blocks.tsv file has one row per continuous block of columns with the same s-exon ID:\n\ncolumn meaning\nalignment which alignment the row describes, for example seed, match, or full\npid the ThorAxe PID threshold used for that seed\ncode the short one-character symbol stored in the MSA\ns_exon_id the original ThorAxe s-exon ID, such as 12_2 or 0_1\nstart_col first MSA column in the block, using 1-based numbering\nend_col last MSA column in the block, using 1-based numbering\nn_columns number of MSA columns in the block\n\nThe Stockholm MSA files also keep the same information as MIToS annotations. SExonCode is one short symbol per MSA column. SExonCodeMap is the key that translates each symbol back to a ThorAxe s-exon ID.\n\n#=GF SExonCodeMap \"a\"=>\"1_0\",\"b\"=>\"12_2\"\n#=GC SExonCode    aaabbb\n\nIn this small example, columns 1 to 3 come from s-exon 1_0, and columns 4 to 6 come from s-exon 12_2.\n\nIf you work directly with MIToS, load Stockholm files with keepinserts=true so insert columns are kept:\n\nusing MIToS.MSA\n\nmsa = read_file(stockholm_path, Stockholm; keepinserts=true)\ncolumn_codes = getannotcolumn(msa, \"SExonCode\")\ncode_key = getannotfile(msa, \"SExonCodeMap\")\n\nThe TSV block table is usually simpler for plotting, because it already groups neighboring columns that belong to the same s-exon.\n\nSome details are important when reading these annotations:\n\n. in SExonCode means Iduna does not assign that column to an s-exon. This usually marks an inserted or unmatched region.\n- gap characters inside an s-exon block still keep that s-exon's annotation, because the column belongs to that s-exon alignment.\nThorAxe IDs that start with 0_ are real ThorAxe s-exon IDs. They often mean that ThorAxe found a segment that did not align with the other s-exons in the pool. If a 0_ s-exon has a protein sequence, Iduna includes and annotates it.\nA 0_ s-exon with no protein sequence adds no MSA columns, so it does not appear as a block in *_s_exon_blocks.tsv.\n\nEach expansion PID directory also has step_state.json, which records the input identity used for safe cache reuse. If the seed MSA, expansion parameters, or centroid request change, Iduna treats the cached expansion as outdated, warns, and rebuilds that PID directory.","title":"S-Exon Column Annotations"}]
}
